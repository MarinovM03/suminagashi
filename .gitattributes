# Normalize line endings to LF in the repo (stops the GitHub Desktop
# "LF will be replaced by CRLF" warning on Windows checkouts).
* text=auto eol=lf

# Treat binary assets as binary so they're never line-ending-converted.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff2 binary
